{
  "name": "@ourai/selfverse",
  "version": "0.0.0",
  "description": "Self is Universe.",
  "private": true,
  "engines": {
    "node": ">=18.20.0"
  },
  "devDependencies": {
    "@knosys/sdk": "^0.0.3",
    "@ntks/toolbox": "^0.0.6",
    "ksio": "^0.0.3"
  },
  "scripts": {
    "init": "node .knosys/scripts init",
    "start": "node .knosys/scripts serve",
    "test": "node .knosys/scripts test",
    "deploy": "node .knosys/scripts deploy"
  }
}
